<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="instatag.com.b3ds">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />

    <meta-data
        android:name="com.facebook.sdk.ApplicationId"
        android:value="@string/FB_id" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterDoctor" />
        <activity android:name=".SetPasswordActivity" />
        <activity android:name=".VerifyRegEmail" />
        <activity android:name=".VerificationChoiceActivity" />
        <activity android:name=".ChoiceActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".SetupProfile" />
        <activity android:name=".ConsultDoctorActivity" />
        <activity android:name=".VisitReportActivity" />
        <activity android:name=".DoctorDashboardActivity" />
        <activity android:name=".ServicesActivity"></activity>
    </application>

</manifest>